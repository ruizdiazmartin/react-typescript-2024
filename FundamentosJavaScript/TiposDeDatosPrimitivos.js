const number = 1;
const string = 'Hola';
const boolean = false;
const undefined = 'undefined';
const nulo = null;

//String interpolacion de texto con una variable
const cadena = `Esto es una cadena concatenada con una variable ${string}`

console.log(number, string, boolean, undefined, null, cadena);